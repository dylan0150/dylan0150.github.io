var app = angular.module("gameApp", ['ngRoute','ngAnimate','app.directive']);

app.config(function ($routeProvider) {
  $routeProvider
    .when('', {
      controller: '',
      templateUrl: ''
    })
});
